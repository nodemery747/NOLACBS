name: stream ritual – wvue, wwl, or misc

on:
  workflow_dispatch:
    inputs:
      target:
        description: "choose which stream to record"
        required: true
        type: choice
        options:
          - wvue
          - wwl
          - misc
      misc_url:
        description: "if target is misc, paste full stream URL here"
        required: false
        type: string

jobs:
  record:
    runs-on: ubuntu-latest
    timeout-minutes: 360

    steps:
      - name: install ffmpeg
        run: sudo apt-get update && sudo apt-get install -y ffmpeg

      - name: record selected stream
        env:
          TARGET: ${{ github.event.inputs.target }}
          MISC_URL: ${{ github.event.inputs.misc_url }}
          WVUE_STREAM_URL: ${{ secrets.WVUE_STREAM_URL }}
          WWL_STREAM_URL: ${{ secrets.WWL_STREAM_URL }}
        run: |
          echo "🎙️ invoking stream ritual for $TARGET at $(date)"
          TARGET=$(echo "$TARGET" | tr '[:upper:]' '[:lower:]' | xargs)

          case "$TARGET" in
            wvue) STREAM_URL="$WVUE_STREAM_URL" ;;
            wwl)  STREAM_URL="$WWL_STREAM_URL" ;;
            misc)
              if [ -z "$MISC_URL" ]; then
                echo "❌ misc_url input is required when target is misc"
                exit 1
              fi
              STREAM_URL="$MISC_URL"
              ;;
            *) echo "❌ invalid stream selection: $TARGET" && exit 1 ;;
          esac

          if [ -z "$STREAM_URL" ]; then
            echo "❌ STREAM_URL is empty. Check your secret or input."
            exit 1
          fi

          echo "🔍 Using stream URL: $STREAM_URL"
          OUTPUT="${TARGET}.mp4"
          echo "📡 recording $TARGET stream for 45m..."
          ffmpeg -nostdin \
            -headers "user-agent: Mozilla/5.0\r\naccept: */*\r\nreferer: https://www.wvue.com\r\norigin: https://www.wvue.com" \
            -reconnect 1 -reconnect_streamed 1 -reconnect_delay_max 2 \
            -fflags +genpts -timeout 3000000 \
            -i "$STREAM_URL" \
            -t 00:45:00 -c copy "$OUTPUT"

          echo "✅ stream ritual complete for $TARGET at $(date)"

      - name: upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: ${{ github.event.inputs.target }}_${{ github.run_id }}
          path: ${{ github.event.inputs.target }}.mp4